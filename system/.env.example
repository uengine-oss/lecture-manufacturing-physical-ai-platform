# 교육 환경 API 키 — 강사가 배포한다. 키가 없으면 HYDOPS_AGENT_MODE=offline 으로 같은 Skill 순서를 코드로 실행한다.
OPENAI_API_KEY=
HYDOPS_LLM_MODEL=gpt-4.1-mini
HYDOPS_EMBEDDING_MODEL=text-embedding-3-small
# HYDOPS_AGENT_MODE=offline
